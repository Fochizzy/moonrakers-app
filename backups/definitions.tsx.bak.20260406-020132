import React from "react";
import {
  SafeAreaView,
  ScrollView,
  StyleSheet,
  Text,
  View,
} from "react-native";

import StarryNight from "@/components/ui/StarryNight";

const COLORS = {
  bg: "#081120",
  card: "rgba(12,18,38,0.92)",
  cardAlt: "rgba(16,24,48,0.95)",
  text: "#E2E8F0",
  sub: "#94A3B8",
  muted: "#64748B",
  accent: "#A855F7",
  accentSoft: "rgba(168,85,247,0.18)",
  blue: "#3B82F6",
  border: "rgba(255,255,255,0.08)",
};

const DEFINITIONS = [
  {
    title: "ELO",
    body: "A rolling rating that updates based on match results and opponent strength.",
  },
  {
    title: "Total Prestige",
    body: "Combined prestige output from direct actions, assists, and any scored sources tracked in your game totals.",
  },
  {
    title: "Direct Prestige",
    body: "Prestige earned directly by the player rather than received through assists.",
  },
  {
    title: "Assist Prestige",
    body: "Prestige gained from teammate assistance or assist-related scoring transfers.",
  },
  {
    title: "Win Rate",
    body: "Wins divided by games played.",
  },
  {
    title: "Efficiency",
    body: "Prestige output relative to contracts and assists. Higher means more value per action.",
  },
];

export default function DefinitionsScreen() {
  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.backgroundLayer}>
        <StarryNight />
        <View style={styles.backgroundDim} />
      </View>

      <ScrollView
        contentContainerStyle={styles.contentContainer}
        showsVerticalScrollIndicator={false}
      >
        <View style={styles.headerCard}>
          <View style={styles.headerTextWrap}>
            <Text style={styles.kicker}>Moonrakers</Text>
            <Text style={styles.title}>Definitions</Text>
            <Text style={styles.subtitle}>
              Quick glossary for the main metrics used across Home, ELO, and Leaderboard.
            </Text>
          </View>

          <View style={styles.headerBadge}>
            <Text style={styles.headerBadgeText}>GUIDE</Text>
          </View>
        </View>

        <View style={styles.list}>
          {DEFINITIONS.map((item) => (
            <View key={item.title} style={styles.card}>
              <Text style={styles.cardTitle}>{item.title}</Text>
              <Text style={styles.cardBody}>{item.body}</Text>
            </View>
          ))}
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: COLORS.bg,
  },

  backgroundLayer: {
    ...StyleSheet.absoluteFillObject,
  },

  backgroundDim: {
    ...StyleSheet.absoluteFillObject,
    backgroundColor: "rgba(2,6,23,0.34)",
  },

  contentContainer: {
    padding: 8,
    paddingBottom: 20,
    gap: 8,
  },

  headerCard: {
    backgroundColor: COLORS.card,
    borderRadius: 14,
    padding: 10,
    borderWidth: 1,
    borderColor: COLORS.border,
    marginBottom: 6,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "flex-start",
  },

  headerTextWrap: {
    flex: 1,
    paddingRight: 12,
  },

  kicker: {
    color: COLORS.blue,
    fontSize: 11,
    fontWeight: "800",
    textTransform: "uppercase",
    letterSpacing: 0.7,
    marginBottom: 4,
  },

  title: {
    color: COLORS.text,
    fontSize: 22,
    fontWeight: "900",
    lineHeight: 24,
  },

  subtitle: {
    color: COLORS.sub,
    fontSize: 11,
    lineHeight: 15,
    marginTop: 6,
  },

  headerBadge: {
    backgroundColor: COLORS.accentSoft,
    borderRadius: 999,
    paddingHorizontal: 10,
    paddingVertical: 6,
  },

  headerBadgeText: {
    color: COLORS.accent,
    fontSize: 11,
    fontWeight: "800",
  },

  list: {
    gap: 8,
  },

  card: {
    backgroundColor: COLORS.cardAlt,
    borderRadius: 14,
    padding: 12,
    borderWidth: 1,
    borderColor: COLORS.border,
  },

  cardTitle: {
    color: COLORS.text,
    fontSize: 15,
    fontWeight: "800",
    marginBottom: 6,
  },

  cardBody: {
    color: COLORS.sub,
    fontSize: 12,
    lineHeight: 18,
  },
});
